import React from 'react';
import { Link } from 'react-router-dom';

export const Homepage = () => {
  return (
    <p>Esta es la pagina de subastas que tenemos que loco no crees</p>
  );
};

export default Homepage;